<?xml version="1.0" encoding="UTF-8" ?>

<Config>

<Resource name='sql.forecast'>
SELECT
   DATE_FORMAT(RECORD_DAY, '%Y%m%d') RECORD_DAY,
   SUM(INVESTMENT) INVESTMENT,
   MAX(INVESTMENT_TOTAL) INVESTMENT_TOTAL,
   SUM(REDEMPTION) REDEMPTION,
   SUM(NETINVESTMENT) NETINVESTMENT,
   SUM(INTEREST) INTEREST
FROM ST_FIN_PHASE
GROUP BY RECORD_DAY
ORDER BY RECORD_DAY
</Resource>

</Config>
<?xml version="1.0" encoding="UTF-8"?>

<Config>

   <!-- ============================================================ -->
   <!-- 设计：资源预览数据获取处理                                   -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.resource.preview'
         face  = 'org.mo.content.face.resource3d.resource.IPreviewServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.resource.IPreviewServlet'
         class = 'org.mo.content.face.resource3d.resource.FPreviewServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：位图数据处理                                           -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content2d.bitmap'
         face  = 'org.mo.content.face.resource2d.bitmap.IBitmapServlet'/>

   <Component
         face  = 'org.mo.content.face.resource2d.bitmap.IBitmapServlet'
         class = 'org.mo.content.face.resource2d.bitmap.FBitmapServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：位图图像数据处理                                       -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content2d.bitmap.image'
         face  = 'org.mo.content.face.resource2d.bitmap.IBitmapImageServlet'/>

   <Component
         face  = 'org.mo.content.face.resource2d.bitmap.IBitmapImageServlet'
         class = 'org.mo.content.face.resource2d.bitmap.FBitmapImageServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：纹理位图数据获取处理                                   -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.texture.bitmap'
         face  = 'org.mo.content.face.resource3d.texture.IBitmapServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.texture.IBitmapServlet'
         class = 'org.mo.content.face.resource3d.texture.FBitmapServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：纹理数据获取处理                                       -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.texture'
         face  = 'org.mo.content.face.resource3d.texture.ITextureServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.texture.ITextureServlet'
         class = 'org.mo.content.face.resource3d.texture.FTextureServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：网格资源获取处理                                       -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.mesh'
         face  = 'org.mo.content.face.resource3d.mesh.IMeshServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.mesh.IMeshServlet'
         class = 'org.mo.content.face.resource3d.mesh.FMeshServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：模型资源获取处理                                       -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.model'
         face  = 'org.mo.content.face.resource3d.model.IModelServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.model.IModelServlet'
         class = 'org.mo.content.face.resource3d.model.FModelServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：模板获取处理                                           -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.template'
         face  = 'org.mo.content.face.resource3d.template.ITemplateServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.template.ITemplateServlet'
         class = 'org.mo.content.face.resource3d.template.FTemplateServlet'
         scope = 'global'/>

   <!-- ============================================================ -->
   <!-- 设计：场景获取处理                                           -->
   <!-- ============================================================ -->
   <WebServlet
         name  = 'cloud.content.scene'
         face  = 'org.mo.content.face.resource3d.scene.ISceneServlet'/>

   <Component
         face  = 'org.mo.content.face.resource3d.scene.ISceneServlet'
         class = 'org.mo.content.face.resource3d.scene.FSceneServlet'
         scope = 'global'/>

</Config>

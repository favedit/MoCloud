<?xml version="1.0" encoding="UTF-8" ?>

<Config>

<Resource name='sql.department'><![CDATA[
SELECT * FROM (
   SELECT
      DEPARTMENT_LABEL LABEL,
      SUM(DEPARTMENT_INVESTMENT) INVESTMENT_AMOUNT,
      SUM(DEPARTMENT_REDEMPTION) REDEMPTION_AMOUNT,
      SUM(MARKETER_COUNT) MARKETER_COUNT,
      MAX(MARKETER_TOTAL) MARKETER_TOTAL,
      SUM(CUSTOMER_COUNT) CUSTOMER_COUNT,
      MAX(CUSTOMER_TOTAL) CUSTOMER_TOTAL
   FROM ST_FIN_DEPARTMENT_PHASE
   WHERE RECORD_MONTH = {month}
   GROUP BY DEPARTMENT_ID
) t
ORDER BY INVESTMENT_AMOUNT DESC
]]></Resource>

</Config>

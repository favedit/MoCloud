<?xml version="1.0" encoding="UTF-8" ?>

<Config>

<Resource name='sql.cockpit.trend'>
SELECT
   RECORD_DAY,
   SUM(INVESTMENT) INVESTMENT_AMOUNT,
   SUM(REDEMPTION) REDEMPTION_AMOUNT
FROM ST_FIN_PHASE
WHERE RECORD_MONTH={month1} OR RECORD_MONTH ={month2}
GROUP BY RECORD_DAY
</Resource>

</Config>

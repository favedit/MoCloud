<?xml version="1.0" encoding="UTF-8" ?>

<Config>

<Resource name='sql.title'>
SELECT
   SUM(INVESTMENT) INVESTMENT_AMOUNT,
   MAX(INVESTMENT_TOTAL) INVESTMENT_TOTAL
FROM ST_FIN_PHASE
WHERE RECORD_MONTH = {month}
</Resource>

<Resource name='sql.marketer'>
SELECT STATUS_CD, COUNT(*) COUNT
FROM ST_FIN_MARKETER
GROUP BY STATUS_CD
</Resource>

</Config>
